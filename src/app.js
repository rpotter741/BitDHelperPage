import { gangData } from "../asset/data";

$(function() {
    var $app = $('#app');
    $app.html('');

    var $header = $('<div class="header"></div>')
    $header.text('Boi\'s Helper App')
    $header.appendTo($app);

})